import { 

} from '../constants/ActionTypes';

const initialState = {
  isWorking: false,
  error: null,
};

export default function monitor(state = initialState, action) {
  switch (action.type) {

  }
   
}
